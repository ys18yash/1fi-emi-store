/**
 * Financial calculation constants and simulation defaults
 * for 1Fi Loan Against Mutual Funds (LAMF) platform.
 */

// Assumed annual CAGR for equity mutual funds over long-term holding period
export const MF_ANNUAL_CAGR = 0.12;

// Standard minimum collateral pledge multiplier for equity mutual funds (1.5x = ~67% LTV)
export const DEFAULT_MIN_PLEDGE_MULTIPLIER = 1.5;
